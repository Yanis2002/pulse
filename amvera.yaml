meta:
  environment: python
  toolchain:
    name: python
    version: 3.11

build:
  commands:
    - pip install --upgrade pip
    - pip install -r requirements.txt

run:
  command: python -m app

env:
  PORT: 8000
  ADMIN_TOKEN: local-admin
